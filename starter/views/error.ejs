<%- include('layout', { body: `
<div class="card">
    <h1>${title || 'Error'}</h1>
    <p>${message || 'An error occurred.'}</p>
    <a href="/" class="btn btn-primary">Return to Home</a>
</div>
` }) %>
